<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.project.EmotionPlanet.Dao.AlarmDao">

	<insert id="insertAlram" parameterType="com.ssafy.project.EmotionPlanet.Dto.AlarmDto">
		insert into alarm(sender, senderNickname, senderImg, receiver, feedno, commentno, pickno, date, type)
		values (#{sender},#{senderNickname},#{senderImg},#{receiver},#{feedno},#{commentno}, #{pickno}, #{date} ,#{type})
		<selectKey keyProperty="no" resultType="int" order="AFTER">
        	SELECT LAST_INSERT_ID()
    	</selectKey>
	</insert>
	
	<delete id="deleteAlram" parameterType="int">
		delete from alarm where
		no = #{no}
	</delete>
	
	<select id="selectAlram" parameterType="int"
		resultType="com.ssafy.project.EmotionPlanet.Dto.AlarmDto">
		select no, sender, senderNickname, senderImg, receiver, feedno, commentno, pickno ,date, type
		from alarm
		where receiver = #{no}
	</select>
</mapper>